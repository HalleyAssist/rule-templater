class RuleParser {
    validate(ruleTemplate, variables = {}){
        // return true if valid, false otherwise
    }

    prepare(ruleTemplate, variables){
        // return a rule string
    }

    extractVariables(ruleString){
        // return a variables object
    }
}
module.exports = RuleParser